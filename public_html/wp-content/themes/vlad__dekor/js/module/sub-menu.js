export const initSubMenu = () => {
    // const menu = document.querySelectorAll('.menu li.sub-menu');
    // menu.forEach((el) => {
    // el.addEventListener('click', (event) => {
    //     event.preventDefault();
    //     const subMenu = el.querySelector('ul');
    //     console.log(subMenu)
    //     subMenu.classList.toggle('is-active');
    // });
    // });
};
